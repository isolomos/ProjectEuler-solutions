#include <iostream>

using namespace std;

int main()
{
	int sum = 0;
	int step = 2;
	int holding = 1;
	for (int i = 0; i < 1000; ++i)
	{
		for (int j = 0; j < 4; ++j)
		{
			holding += step;
			sum += holding;
		}
		step += 2;
		++i;
	}
	cout << sum+1<<'\n';
	
}
